{% extends 'base.html.twig' %}
{#{% block content %}
    <section class="ftco-section ftco-no-pb" style="
    margin-bottom: 132px;
    margin-top: 70px;
">
        <div class="container" style="
    width: fit-content;
">
            <div style="
    width: fit-content;
    margin: auto;
    float: left;
    /* margin-left: 284px; */
"><a href="{{ path('modifierParents',{'id':p}) }}"><button type="" id="" name="" class="btn btn-primary py-3 px-5" value="modifier">Modifier vos informations</button></a></div><div style="
    width: fit-content;
    margin: auto;
    float: left;
    margin-left: 67px;
"><a href="{{ path('ajouter_demande_parent') }}"><button type="" id="projetbundle_parents_Envoyer" name="projetbundle_parents[Envoyer]" class="btn btn-primary py-3 px-5" value="modifier">Demander autorisation !</button></a></div>


            <div style="
    width: fit-content;
    margin: auto;
    float: left;
    margin-left: 67px;
"><a href="{{ path('ajouter_demande_parent') }}"><button type="" id="projetbundle_parents_Envoyer" name="projetbundle_parents[Envoyer]" class="btn btn-primary py-3 px-5" value="modifier">Consulter mes demanndes</button></a></div>

        </div>
    </section>


{% endblock %}#}

{% block content %}

    <section class="ftco-section bg-light">
        <div class="container">
        <div class="row mt">
            <div class="col-md-12">
                <div class="content-panel">
                    <table class="table table-striped table-advance table-hover">
                        <h4><i class="fa fa-angle-right"></i> Table des Demandes d'autorisation</h4>
                        <hr>
                        <thead>
                        <tr>
                            <th><i class="fa fa-bullhorn"></i> ID </th>
                            <th class="hidden-phone">Description </th>
                            <th>Date de sortie</th>
                            <th> Date de retour</th>
                            <th> etat</th>
{#                            <th> Actions</th>#}
                            <th></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for d in demandes%}
                            {% if d.userid == app.user.id %}
                            <tr>
                                <td>
                                    <a>{{ d.id }}</a>
                                </td>
                                <td class="hidden-phone">{{ d.description }}</td>
                                <th><i class=" fa fa-edit"></i> {{ d.DateDeSortie|date("d M Y")  }}</th>
                                <th><i class=" aa-edit"></i> {{ d.dateDeRetour|date("d M Y")  }}</th>
                                <th> {{ d.etat  }}</th>
                               {# <th>  <div class="btn-group btn-group-sm btn-group-horizontal">
                                        <a class="btn btn-secondary"
                                           href="{{ path('gerer_demande', { 'id': d.id ,'token':'Accepte'}) }}"
                                           data-toggle="tooltip" title="Accepter">
                                            <i class="">Accepter</i>
                                        </a>
                                        <a class="btn btn-secondary"
                                                                                           href="{{ path('gerer_demande', { 'id': d.id ,'token':'Refuser'}) }}"
                                           data-toggle="tooltip" title="Refuser">
                                            <i class="">Refuser</i>
                                        </a>
                                    </div>
                                </th>#}
                                <td><span class="label label-info label-mini"></span></td>
                                <td></td>
                            </tr>
                            {% endif %}
                        {% endfor %}
                        </tbody>
                    </table>

                </div>
                <!-- /content-panel -->
            </div>
            <!-- /col-md-12 -->
        </div>
        </div>
        <!-- /row -->
    </section>


{% endblock %}
